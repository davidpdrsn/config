# Config

Personal nix-darwin configuration using flakes.

## Commands

- `make check` - Validate the flake
- `make switch` - Apply changes

## Important

Always run `make check` after making changes to nix files.
