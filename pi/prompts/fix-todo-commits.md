Fix todo commits

On the branch called 'todo' you'll find commits that each correspond to one todo. Fix each by following these steps:

- Move the commit you're working on to a new branch
- Fix it
- When you're done commit to that branch. Don't remove or otherwise change the todo commit

You might need to stack your branches if there dependencies between todos

Only ask questions if something is unclear. Otherwise just go

Use gitbutler
